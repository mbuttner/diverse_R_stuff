# diverse_R_stuff
This is a collection of functions I wanted to have in R, but did not find them. 

## Heatmap triangle plots

This `ggplot2` based visualisation combines two types of information as triangles in a single heatmap. Upper left triangle uses a color map, lower right triangle uses the opacity (appears as grey scale). 
